apiVersion: v1
kind: Service
metadata:
  name: triton-gw
  namespace: triton-gw
spec:
  selector:
    app: triton-gw
  type: LoadBalancer
  ports:
    - name: http
      protocol: TCP
      port: 8000
      targetPort: 8000
---

